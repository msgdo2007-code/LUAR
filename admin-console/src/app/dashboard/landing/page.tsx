import {LandingEditor} from "@/components/admin/landing-editor";import {getLandingEditor} from "@/lib/admin-data";
export default async function LandingPageEditor(){const data=await getLandingEditor();return <section className="admin-page landing-admin-page"><header className="admin-page-head"><div><span>CONTEÚDO PÚBLICO</span><h1>Personalizador da Landing Page</h1><p>Editor seguro por componentes, com rascunho, prévia responsiva e rollback.</p></div><small>JavaScript e HTML arbitrários permanecem bloqueados.</small></header><LandingEditor initial={data.draft} revision={data.draftRevision} publishedRevision={data.publishedRevision} versions={data.versions}/></section>}
